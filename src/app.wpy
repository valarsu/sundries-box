<style lang="less">
@import url('/assets/icon/icon.less');
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  box-sizing: border-box;
}

</style>
<script>
import wepy from 'wepy'
import 'wepy-async-function'
// import {artList2} from './utils/atc2.js'

export default class extends wepy.app {
  config = {
    pages: [
      'pages/index',
      'pages/fitArticle/list',
      'pages/fitArticle/detail',
    ],
    window: {
      backgroundTextStyle: 'light',
      navigationBarBackgroundColor: '#f85415',
      navigationBarTitleText: 'WeChat',
      navigationBarTextStyle: 'white'
    },
    tabBar: {
      list: [{
        pagePath: 'pages/index',
        text: '打卡',
        iconPath: 'assets/imgs/clockInRoot.png',
        selectedIconPath: 'assets/imgs/clockIn.png'
      }, {
        pagePath: 'pages/fitArticle/list',
        text: '文章',
        iconPath: 'assets/imgs/listRoot.png',
        selectedIconPath: 'assets/imgs/list.png',
      }]
    }
  }

  globalData = {
    userInfo: null,
    testDB: null
  }

  constructor () {
    super()
    this.use('requestfix')
  }

  onLaunch() {
    // this.initCloudService()
    wx.cloud.init({
      env: 'box-test-2e622d'
    })
    this.globalData.testDB = wx.cloud.database({
      env: 'box-test-2e622d'
    })
    // console.log(articles.get({
    //   success(res) {
    //     console.log(res.data)
    //   }
    // }))
    // console.log(artList2)
    // this.globalData.testDB = wx.cloud.database({
    //   env: 'box-test-2e622d'
    // })
    // let articles = this.globalData.testDB.collection('articles')
    // artList2.forEach((item, idx) => {
    //   articles.add({
    //     data: {
    //       index: 20 + idx,
    //       detail: item,
    //       due: new Date()
    //     }
    //   })
    // })
    // console.log(this.getDBData())
    // this.getDBData()
  }

  getDBData() {

    // let articles = this.globalData.testDB.collection('articles')
    // const _ = this.globalData.testDB.command
    // articles.get().then(res => {
    //   console.log(res.data)
    // })
    // articles
    //   .where({
    //     index: _.gte(0).and(_.lte(19))
    //   })
    //   .get()
    //   .then(res => {
    //     console.log(res.data)
    //   })
  }
  getUserInfo(cb) {
    const that = this
    if (this.globalData.userInfo) {
      return this.globalData.userInfo
    }
    wepy.getUserInfo({
      success (res) {
        that.globalData.userInfo = res.userInfo
        cb && cb(res.userInfo)
      }
    })
  }

}
</script>
